<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>"><i class="fa fa-code" aria-hidden="true" style="color: orange"></i> LeSnippets</a>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(snippets_path) %>"><a href="<%= snippets_path %>">Snippet Toolbox</a></li>
        <li class="<%= 'active' if current_page?(new_snippet_path) %>"><a href="<%= new_snippet_path %>"> Add Snippet</a></li>
        <li class="<%= 'active' if current_page?(languages_path) %>"><a href="<%= languages_path %>"> Snippets by Language</a></li>
      </ul>



      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="navbar-text"><%= current_user.first_name %></li>
          <li><%= link_to "Logout", sessions_path, method: :delete,
                      data: {confirm: "are you sure you want to log out?"}%></li>
        <% else %>
          <li class="<%= 'active' if current_page?(new_session_path) %>"><a href="<%= new_session_path %>">Login</a></li>
          <li class="<%= 'active' if current_page?(new_user_path) %>"><a href="<%= new_user_path %>">Sign Up</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
